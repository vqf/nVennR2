% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{estimateExhaustiveRunTime}
\alias{estimateExhaustiveRunTime}
\title{Estimates the time taken by minimization steps when preparing a diagram with
the exhaustive method at a given depth}
\usage{
estimateExhaustiveRunTime(desc, maxlevel = 0L, byCol = 0L)
}
\arguments{
\item{desc}{Description of sets, either as a file path, a list of lists, text or a
previously created nVenn object (see \code{\link[=nVennDiagram]{nVennDiagram()}}).}

\item{maxlevel}{Depth of the exhaustive search. See the \code{maxlevel} parameter
of \code{\link[=nVennDiagram]{nVennDiagram()}} for details.}

\item{byCol}{If the input is a text, this parameter indicates whether
each set is a column (1) or a row (2). Defaults to 0, which means that
the package will try to guess which possibility makes more sense.}
}
\value{
float Estimated time for steps 3 and 4 in seconds. In case of
error, the returned value is -1.
}
\description{
Estimates the time taken by minimization steps when preparing a diagram with
the exhaustive method at a given depth
}
\examples{
estimateExhaustiveRunTime(exampledf, 4)
}
